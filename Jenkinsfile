pipeline {
    agent any
    stages {
        stage('run python') {
            steps {
                script {
                    sh "ls -la" 
                    sh "python3 --version" 
                }
            }
        }
    }
}
